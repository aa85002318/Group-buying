import { ShopCmsStudio } from "@/components/admin/shop/ShopCmsStudio";

/** Shop homepage CMS hub — studio shell with section list + live preview. */
export default function AdminShopCmsHubPage() {
  return <ShopCmsStudio />;
}
